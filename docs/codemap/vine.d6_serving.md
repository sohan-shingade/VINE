# Codemap: `vine.d6_serving`

> Generated by `scripts/codemap.py` at git `ac03b6a`. Do not edit;
> regenerate after any refactor (same commit). Consult before Reading source.

### `src/vine/d6_serving/__init__.py`
*D6 — containerized FastAPI inference services deployed on NRP Kubernetes.*


### `src/vine/d6_serving/app.py`
*FastAPI inference service for D6 model predictions.*
imports: `vine`, `vine.common`, `vine.common.config`, `vine.d1_pipeline.ingest`, `vine.d6_serving.irrigation`

- const `_SERVING_CONFIG_PATH`
- const `_SERVING_CONFIG`
- `def healthz() -> dict[str, str]` — Return process health for Kubernetes probes.
- `def irrigation_forecast(block_id: str) -> IrrigationForecast` — Return a persistence forecast and threshold advice for a vineyard block.
- `def harvest_readiness(block_id: str) -> dict[str, object]` — Return the placeholder harvest-readiness contract for a block.

### `src/vine/d6_serving/irrigation.py`
*Pure irrigation-serving logic for the shipped D2 persistence model.*

- **class IrrigationServingConfig(BaseModel)**
- **class HorizonForecast(BaseModel)**
- **class IrrigationForecast(BaseModel)**
- **class UnknownBlockError(LookupError)** — Requested block has no configured sensor.
- **class NoReadingError(RuntimeError)** — Configured sensor has no usable moisture reading.
- `def build_irrigation_forecast(block_id: str, cfg: IrrigationServingConfig, load_device: Callable[[str], pd.DataFrame], *, now: datetime | None = None) -> IrrigationForecast` — Build a quality-aware persistence forecast from the newest valid reading.

