# Codemap: `vine.d3_vision`

> Generated by `scripts/codemap.py` at git `45aeed6`. Do not edit;
> regenerate after any refactor (same commit). Consult before Reading source.

### `src/vine/d3_vision/__init__.py`
*D3 — plant-health computer vision on multispectral imagery.*


### `src/vine/d3_vision/config.py`
*Typed config for CV experiments (validates configs/d3_vision/*.yaml).*

- **class ChannelSpec(BaseModel)** — One input channel, sourced from a single band of one raster.
- **class CVConfig(BaseModel)** — Config for a D3 patch-classification run.
    - `def validate_shape(self) -> CVConfig`

### `src/vine/d3_vision/model.py`
*CNN with an N-channel input adapter over an ImageNet-pretrained backbone (D3).*
imports: `vine.d3_vision.config`

- `def adapt_stem(conv: Any, in_channels: int) -> Any` — Return a copy of stem conv `conv` that accepts `in_channels` channels.
- `def build_model(cfg: CVConfig) -> nn.Module` — Construct the fine-tuning model from a CVConfig.

### `src/vine/d3_vision/pseudolabel.py`
*Pseudo-labelled patch dataset for D3 plant-health CV.*
imports: `vine.common.logging`, `vine.d1_pipeline.geo`, `vine.d3_vision.config`

- `def tile_origins(height: int, width: int, patch_size: int) -> list[tuple[int, int]]` — Top-left offsets of a non-overlapping `patch_size` grid over height/width.
- `def quantile_thresholds(values: Sequence[float], quantiles: Sequence[float]) -> tuple[float, ...]` — Class boundaries at the given quantiles of `values` (pure).
- `def assign_labels(values: Sequence[float], thresholds: Sequence[float]) -> NDArray[np.int64]` — Bin `values` into class indices 0..len(thresholds) by ascending threshold.
- `def split_blocks(block_ids: Sequence[str], val_fraction: float, seed: int) -> tuple[list[str], list[str]]` — Partition unique block IDs into train/val groups (pure, deterministic).
- `def _block_windows(cfg: CVConfig, source, geometry) -> list[tuple[int, int]]` — Row/col offsets of patches fully inside one block polygon and the raster.
- `def _read_patch(sources, cfg: CVConfig, row: int, col: int)` — Read one aligned multi-channel patch; returns (data, valid_fraction).
- `def build_patch_cache(cfg: CVConfig) -> tuple[Path, pd.DataFrame]` — Tile block interiors into patches and cache them next to an index frame.
- `def load_patch_cache(cfg: CVConfig) -> tuple[Path, pd.DataFrame]` — Return the cached patch stack and index, building them if absent.

### `src/vine/d3_vision/stress.py`
*Typed configuration for label-free D3 vegetation-index screening.*
imports: `vine.d1_pipeline.geo`

- **class StressRankingConfig(BaseModel)**
    - `def validate_weights(self) -> StressRankingConfig`
- `def _concern_percentile(values: pd.Series) -> pd.Series` — Percentile concern score where smaller index values rank higher.
- `def rank_stress_candidates(ndvi: pd.DataFrame, ndre: pd.DataFrame, cfg: StressRankingConfig) -> pd.DataFrame` — Rank blocks for field review from same-acquisition NDVI/NDRE summaries.
- `def screen_rasters(cfg: StressRankingConfig) -> pd.DataFrame` — Read NDVI/NDRE windowed by block and return the stress-screening table.

