# Codemap: `vine.common`

> Generated by `scripts/codemap.py` at git `3514cea`. Do not edit;
> regenerate after any refactor (same commit). Consult before Reading source.

### `src/vine/common/__init__.py`
*Cross-cutting utilities: config, logging, reproducible seeding.*
imports: `vine.common.config`, `vine.common.logging`, `vine.common.seed`


### `src/vine/common/config.py`
*Configuration: environment settings + typed YAML experiment configs.*

- const `REPO_ROOT`
- **class Settings(BaseSettings)** — Environment/process configuration. Read once at import time.
- `def load_config(path: str | Path) -> dict[str, Any]` — Load a YAML experiment config into a dict.

### `src/vine/common/logging.py`
*Structured logging. One configured logger factory for the whole project.*
imports: `vine.common.config`

- `def _configure() -> None`
- `def get_logger(name: str) -> structlog.stdlib.BoundLogger` — Return a bound structured logger, e.g. `log = get_logger(__name__)`.

### `src/vine/common/seed.py`
*Reproducible seeding. Call `seed_everything()` at the top of every run.*
imports: `vine.common.config`

- `def seed_everything(seed: int | None = None) -> int` — Seed Python, NumPy, and (if installed) PyTorch. Returns the seed used.

