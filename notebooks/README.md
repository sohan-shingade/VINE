# Notebooks

Exploratory analysis only — **not** the source of truth. Anything worth keeping
graduates into `src/vine/` with tests. Name notebooks `NN-topic.ipynb`
(e.g. `01-sensor-eda.ipynb`). Clear outputs before committing (pre-commit
checks file size). Reproducible logic belongs in the package, not here.
