# Scripts

One-off operational scripts (data download, DVC setup, MLflow server bootstrap
on NRP). Reusable logic belongs in `src/vine/` and is exposed via the `vine`
CLI; this folder is for glue that isn't part of the package.
